export interface User {
  id: string;
  reputation: string;
  display_name: string;
  location: string;
  views: string;
  up_votes: string;
  down_votes: string;
  profile_image_url?: string;
  account_id: string;
}
